import React from "react";
import EditCard from "components/profile/editCard"

type PropTypes = {
  location: any,
};

const CollectionEditCard = (props: PropTypes) => {
  return (
    <EditCard />
  );
};

export default CollectionEditCard;